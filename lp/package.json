{
  "name": "lp",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "predev": "npm run build -w @teritamas/shared",
    "dev": "astro dev",
    "prebuild": "npm run build -w @teritamas/shared",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "precheck": "npm run build -w @teritamas/shared",
    "check": "astro check"
  },
  "dependencies": {
    "@astrojs/check": "^0.9.8",
    "@tailwindcss/vite": "^4.2.2",
    "@teritamas/shared": "0.0.1",
    "astro": "^6.1.3",
    "tailwindcss": "^4.2.2"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
